/**
 * Index file for test mocks
 * Provides a cleaner way to import mocks in tests
 */

// Re-export from the n8n mocks
export * from './n8n/MockInterfaces';

// Firebase mocks are handled via Jest module mocks, no need to export them